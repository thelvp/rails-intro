<h1>This is the home page</h1>

<p>Look, a home! 🏠</p>

<p>Using the pe tag to insert my ruby calculation of 2 + 3:</p>
<%= 2 + 3 %>

<%# link_to("name of link", where should it go --> rails routes 'prefix'+_path) %>
<p>Inserting a link with the link_to() method: <%= link_to("go to Contact", contact_path) %> </p>
